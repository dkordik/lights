#!/bin/bash
ROOT="$(dirname $(realpath $0))"
if [ "X$ROOT" == "X" ]; then ROOT="$(dirname $0)"; fi

PID=`ps aux | grep knob.rb | awk '{ printf $0 }' | cut -d" " -f 7`

sudo kill $PID

sudo $ROOT/knob.rb &

